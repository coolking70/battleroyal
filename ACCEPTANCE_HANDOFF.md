# 独立验收交接文档

给接手本项目验收工作的 AI。

本文不复述项目功能（见 `README` 与各阶段 `PHASE*_REPORT.md`），只交接
**验收所需的判据、方法与坑**。

---

## 1. 你的角色

你是**独立验收方**，不是开发方。开发由另一个 AI 负责，你的职责是：

- 独立核实其交付，而非采信其报告
- 按项目方（人类）的指示写下一阶段的开发提示词
- 在发现缺陷时如实报告，包括自己此前漏掉的

**核心原则:源码与实际运行证据优先于开发方自述。** 开发报告写 PASS
不等于 PASS;测试全绿也不等于需求达成——本项目已多次出现"指标达标但本意未达成"。

---

## 2. 当前基线（截至 main@22a3654）

```
版本          package.json 0.4.0 === GAME_VERSION 0.4.0（两者必须一致）
测试          88 files / 1446 tests
正式美术      35 张 PNG，必须逐字节不变
存档校验      89 例损坏用例
core 最大文件  commandHandlers.ts（500 行，已卡上限）
分支          main + agent/phase3a2-closure（后者已并入，可删）
```

## 3. 已建立的不可退让约束

这些是十余轮逐条建立、且**踩过坑才补上的**。任何阶段都不得突破，
写提示词时必须逐条列出——开发方不会自己记得。

### 3.1 零体力不变量（4C-3）

`tests/phase4c3ZeroStaminaDeadlock.test.ts` **必须全绿且断言不得修改**。

背景：`actionCosts.ts` 曾承诺"脱离免费，从根上消灭零体力死锁"，但
`fleeDestinations` 排除禁区，而 `minSafeZones: 1` 保证终局只剩一个安全区——
届时逃跑自动失败且敌人白打一下。已修复为"原地脱离"，并让防御在
**恰好 0 体力**时免费。

**衍生约束**：任何新动作都不得在零体力时免费。4F-1 的经验发放规定
"只在支付正体力时给经验"，若新技能免费，会同时打开死锁与经验刷取两个口子。

### 3.2 信息边界

本项目的核心设计支柱是"信息不完全"，已建立多层边界：

```
禁止暴露：NPC 位置 / 意图 / 背包 / 等级 / 经验
          zone.loot 实际剩余库存
          未发现的地面掉落、敌方精确 HP
          未来事件、未公布禁区
既定公开：ZONE_EXHAUSTED、禁区预警、世界事件、supplyLabel（物资分档）
          敌方无数字 HP 条 + 文字描述、武器、EXPOSED、共享命中/脱离率
```

**关键技法**：日志渲染必须经 `visibleEventsForPlayer`。修复某条边界后，
**必须搜索所有同类数据源**——4C-8 的泄露正是因为 `ResultScreen` 存在一条
平行的未过滤 `state.events` 读取路径。

### 3.3 架构指标（4D-2 / 4D-3 建立）

1280×720 探索态实测，写提示词时作为硬指标：

```
常驻信息块        5     （topbar / zone-rail / zone-hero / craft-goal-bar / actionbar）
首屏空态文案      0     （不得出现"还没有""暂无""空槽"等占位）
实时 DOM [title]  0     （禁用原因不得只靠 hover）
遭遇战 7 个动作   免滚动全部可见（1280×720 / 390×844 / 844×390 均已达成）
五视口横向溢出    无
```

注意：脚本统计常驻块常得 6,其中 `.panel.stage` 是包住 hero 与目标条的容器,
非独立信息块。

### 3.4 其他

- **物品守恒**：`auditItemIntegrity` 逐 tick 守恒必须通过
- **确定性**：同种子同操作序列结果一致;新功能不得引入未隔离的随机抽取
- **行数**：`audit:deps` R4 限制 core/data 单文件 ≤500 行,
  `commandHandlers.ts` 已卡上限——加东西前先拆分
- **美术**：35 张 PNG 逐字节不变;新物品走 `VisualImage` 三级降级,
  **禁止补图**（rain 无正式图是已记录的 provider 例外）

---

## 4. 验收方法

### 4.1 固定流程

```
1. 查 CI 是否在当前 HEAD 上 success（headSha 要对上,不能只看"最近一次绿"）
2. 查 scope：git diff --name-only <base>..HEAD -- src/core src/data public/assets
3. 查被修改的既有测试是否被削弱（最容易被忽略的一环）
4. 本地跑全部门禁（见下）
5. 运行时独立验证（干净生产构建,不要用长驻 dev server）
6. 清理工作区,还原门禁产生的报告改动
```

### 4.2 门禁清单

```
npm run typecheck
npm test
npm run build
npm run audit:save        npm run audit:deps
npm run art:doctor -- --offline    npm run art:validate    npm run art:audit:phase4a
npm run art:security:browser       npm run art:security:repo
npm run simulate -- --games 500 --seed-prefix <PHASE> --regression
npm audit --omit=dev
```

**别漏跑 `audit:deps`**——我曾因只跑 typecheck/test/build 而漏掉行数超限,
被 CI 兜住。

### 4.3 运行时验证

用 `npm run build` + `npm run preview` 的**干净生产构建**采证,
不要用长驻 dev server（HMR 残留报错会被误判为代码缺陷,4B-1 发生过）。

React 状态更新需要跨工具调用才能渲染。同一次 `javascript_exec` 内连续点击
不会重渲染;要么一次调用一个动作,要么用 `await new Promise(r=>setTimeout(r,120))`。

### 4.4 工作区卫生

跑门禁会重写 `reports/` 下多个文件。验收结束前用
`git checkout -- reports/` 还原,但**保留** `reports/save-validation-audit.*`
的时间戳级改动（它们自 Phase 4A 起就不再提交,是既定状态）。

---

## 5. 开发方的行为特征

**擅长**：忠实执行精确规格。规格写得越硬,交付质量越高。
会主动做非显然但正确的判断并写明理由（如按 itemId 而非堆叠聚合恢复道具候选）。

**不擅长**：质疑前提。它会把你的规格当作既定结论去实现,不会检验其合理性。

**已验证的良好习惯**：遇到范围冲突会停下来问,而不是硬做或擅自扩大。
这个习惯要保护——提示词里始终保留"遇到分歧先说明并等确认"一节。

### 已发生过的失败模式

1. **满足指标但偏离本意**：我要求"Lv.5 达成率 ≤10%",它调出 0%,
   五级系统退化成三级。**写区间不要只写单边界。**
2. **未声明依赖**：`@playwright/test` 装了但没写进 package.json,
   干净检出 typecheck 失败、CI 变红。**要求从 `npm ci` 干净路径验证。**
3. **测量器缺陷未追到底**：4J-0 察觉"装备率 0%"却当作背景条件,
   没追到"选项默认关闭"这一层。

---

## 6. 我漏掉过的东西（重点参考）

这些是我作为验收方的实际盲区,交接给你时一并列出：

1. **平行数据路径**：4B-4 修了 EventLog 的过滤,但 `ResultScreen` 有另一条
   未过滤路径,直到 4C-8 才被开发方发现。**修边界后要搜全部同类读取点。**
2. **写入顺序**：4E-1 把击杀行写进了 `encounter.log`,测试也通过,
   但 UI 读的是最后一条,击杀行被伤害行压住——**测试断言"写进去了",
   玩家看的是"最后一条",中间有缝。**
3. **远处地面掉落**：`ZoneMap` 曾对所有区域显示掉落数,我在 4B-0 见过
   还写进了 UI 清单,没质疑远处掉落是否该可见。
4. **视觉挤压**：4I-1 我测了"7 个按钮免滚动可达"（达标）,
   但没检查两个技能是否被挤在同一格（字号 10)。**可达 ≠ 可用。**
5. **行数上限**：我把两个字段展开成带注释的四行,把 `types.ts` 推到 503 行,
   CI 失败。开发方原本挤在一行**是有原因的**。

---

## 7. 待办与已知状态

### 需要处理

- **`PHASE4J0_DIAGNOSIS.md` 结论部分失效**。它基于"自动玩家不装备"的失真数据。
  4J-1 已在其报告中逐条重评,但原文档未改写。
  **不要据其调参数**,建议下一轮基于可信测量器重做诊断。
- **`commandHandlers.ts` 卡在 500 行**,加东西前须先拆分。
- **平衡从未调过**。4J-1 修复后 500 局胜率 9.0%、角色平衡首次 PASS。
  项目方尚未确认 9% 是否是想要的基线——这是设计判断,不要替他决定。

### 长期挂账（需人工试玩）

- 真机触控与安全区行为
- 读屏措辞与播报时机
- 长时对局的面板密度与 reduced-motion 舒适度
- 4G-2「击杀者优先拾取」的手感（自动玩家不会为抢夺调整行为）
- Lv.3 解锁第二技能的时机感

### 关于自动化的局限

`tools/autoPlayer.ts` 的五种策略**测不到 UI 引导**（它不看界面）,
此前也测不到装备（4J-1 已修）。**任何 UI 层改动的成效都无法用它验证**,
必须靠人工试玩。写提示词时若要设"成效指标",先确认该指标结构上测得到——
我曾要求用它验证合成引导成效,结果得到的是构造性零结果。

**本项目至今最有价值的改动全部来自人类试玩反馈**,自动化门禁一个都没抓到:
零体力死锁、日志泄露、对战框冗余、击杀战报缺失、满级过快、战利品数量不实。

---

## 8. 协作约定

- **合并流程**：改动 → 提交推送 → CI 绿 → 开 PR（正文写清根因与实测数字）
  → 合并（保留提交历史,用 merge commit）→ 删分支 → 确认 main CI
- **PR 正文**由验收方重写,开发方常只写阶段编号。正文要如实包含
  未验证项与已知遗留,不要只写成功项。
- **证据留存**：截图等二进制留本地,仓库只提交 JSON 数值快照
  （见 `CONTRIBUTING.md`;`output/` 已 gitignore,曾有一轮强制提交 3.4MB 被剔除）
- **规划与设计由人类决定**。项目方明确表示设计阶段由人类与验收方共同完成,
  开发方只负责实施。写提示词时把设计决策写死,并注明"已冻结,不要重新论证"。
