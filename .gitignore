# 依赖
node_modules/

# 构建产物
dist/
*.tsbuildinfo

# 本地开发工具产生的记录（非项目代码）
.workbuddy/

# 本地 git worktree（开发者机器上的临时检出，绝不入库）
.worktrees/

# 编辑器 / 系统
.DS_Store
.idea/
.vscode/
*.swp

# 日志与临时文件
*.log
npm-debug.log*
*.tmp
*.bak
*.orig

# 环境变量
.env
.env.local
.env.*.local

# Phase 4 local AI art production state (never commit generated candidates/cache)
.art-cache/
art/candidates/**
art/archive/
art/reviews.json
output/

# Playwright 证据输出（不入库）
test-results/
playwright-report/

# Phase 4X 中间调参轮次的完整 JSON（保留 .md 摘要即可，避免入库巨型生成文件）
reports/p4x-tune*.json
