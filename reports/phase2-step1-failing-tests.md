# 第一步：失败测试基线记录

命令：`npx vitest run tests/phase2-hardening.test.ts`
执行时间：2026-08-07 · 代码状态：第一阶段原始代码（未做任何硬化改动）

## 结果总览

```
 ❯ tests/phase2-hardening.test.ts (9 tests | 8 failed)

 Test Files  1 failed (1)
      Tests  8 failed | 1 passed (9)
```

## 逐条对照

| # | 用例 | 期望 | 当前实际 | 状态 |
| --- | --- | --- | --- | --- |
| 1 | 零体力时不能移动 | `ok === false` | `ok === true`，玩家照常移动并推进时间 | ❌ 失败（符合预期） |
| 2 | 零体力时不能攻击 | `ok === false` | `ok === true`，攻击照常结算并扣血 | ❌ 失败（符合预期） |
| 3 | 区域物资有限、可被搜空 | 300 次搜索最多产出 33 件；搜空后再搜 30 次产出 0 件 | 产出远超上限，永远搜得到东西 | ❌ 失败（符合预期） |
| 4 | 损坏存档必须被拒绝 | 6 种损坏形态全部 `false` | 浅校验全部放行，返回 `true` | ❌ 失败（符合预期） |
| 5 | 持续伤害触发死亡结算 | `hp=0 / alive=false / deathOrder 含玩家 / status='lost'` | `hp=0` 但 `alive` 仍为 `true`，对局继续 | ❌ 失败（符合预期） |
| 6 | 非法配方不抛异常 | 返回 `ok:false` | `getRecipe` 抛出，引擎未捕获 | ❌ 失败（符合预期） |
| 7 | 非法物品不抛异常 | 返回 `ok:false` | `getItem` 抛出，引擎未捕获 | ❌ 失败（符合预期） |
| 8 | 未解决的遭遇不能关闭 | `ok:false`，遭遇保留 | `ok:true`，遭遇被直接清空 | ❌ 失败（符合预期） |
| 9 | 已解决的遭遇可以关闭 | `ok:true`，遭遇清空 | 同期望 | ✅ 通过（作为对照组） |

第 9 条是**对照用例**：它现在就通过，用于确保后续修复不会矫枉过正（把合法的关闭也一起禁掉）。

## 结论

8 个失败用例精确复现了 `PHASE2_BASELINE.md` §6 中记录的缺陷，覆盖 P0-1 / P0-2 / P0-3 / P0-4 / P1-5 / P1-6 六项。
第二步与第三步的改造必须让这 8 条全部转绿，同时保持原有 71 个用例不变。
